import react from 'react';
import styles from '@/styles/components/Reels.module.scss';

const Reels = () => {
  return (
    <div className={styles.reelsContainer}>
        Reels container
    </div>
  );
};

export default Reels;
